APP_NAME="Presensi Siswa SMKN 1 Bantul"
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=http://localhost
APP_TIMEZONE=Asia/Jakarta
APP_LOCALE=id

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=presensi_siswa
DB_USERNAME=root
DB_PASSWORD=

BROADCAST_DRIVER=log
CACHE_DRIVER=redis
FILESYSTEM_DISK=local
QUEUE_CONNECTION=redis
SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_DOMAIN=null
SESSION_SECURE_COOKIE=false

MEMCACHED_HOST=127.0.0.1

REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_HOST=mailhog
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

# Sanctum Configuration
SANCTUM_STATEFUL_DOMAINS=localhost,127.0.0.1
SANCTUM_GUARD=web

# CORS Configuration
CORS_ALLOWED_ORIGINS=*
CORS_ALLOWED_METHODS=*
CORS_ALLOWED_HEADERS=*

# Attendance System Configuration
ATTENDANCE_CHECK_IN_START=06:00
ATTENDANCE_CHECK_IN_END=09:00
ATTENDANCE_CHECK_IN_LATE=07:30
ATTENDANCE_CHECK_OUT_START=14:00
ATTENDANCE_CHECK_OUT_END=17:00
ATTENDANCE_QR_EXPIRY_MINUTES=1440
ATTENDANCE_QR_SIZE=300
ATTENDANCE_LOCATION_ENABLED=true
ATTENDANCE_LOCATION_REQUIRED=true
ATTENDANCE_SCHOOL_LATITUDE=-6.2088
ATTENDANCE_SCHOOL_LONGITUDE=106.8456
ATTENDANCE_RADIUS_METERS=100
ATTENDANCE_PHOTO_ENABLED=true
ATTENDANCE_PHOTO_REQUIRED=true
ATTENDANCE_PHOTO_MAX_SIZE_KB=2048

# Academic Configuration
ACADEMIC_YEAR=2024
ACADEMIC_SEMESTER=1

# Media Library
MEDIA_DISK=public

# L5 Swagger
L5_SWAGGER_GENERATE_ALWAYS=false
L5_SWAGGER_CONST_HOST=http://localhost:8000/api
